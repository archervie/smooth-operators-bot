[project]
name = "smooth-operators"
version = "0.0.4"
description = "A Discord bot developed for use in the Jojodoss Discord server."
authors = [
    {name = "archervie",email = "archervie@proton.me"}
]
license = "MIT"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "discord-py (>=2.7.1,<3.0.0)",
    "dotenv (>=0.9.9,<0.10.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
