services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: smooth-operators
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/__pycache__
